/*
	Header file of calc_methods.
	Here, I define the 4 functions that are going to use 
		in calc.cpp (the main program).
	The implementations of the 4 functions are
		in calc_methods.cpp
 */

// If some term is not defined
#ifndef CALC_METHODS

// We define this term. This is used to avoid 
#define CALC_METHODS


// The *definitions* only.
int add(int a, int b);

int sub(int a, int b);

int mult(int a, int b);

int divide(int a, int b);

// End IF.
#endif